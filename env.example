# Anthropic API Key
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Path to FHL-MCP-Server (defaults to ./FHL-MCP-Server)
# FHL_SERVER_PATH=/path/to/FHL-MCP-Server

# Logging Configuration
# Enable conversation logging (default: true)
ENABLE_LOGGING=true

# Log format: "json", "text", or "both" (default: "both")
# - json: Structured JSON format, easy for programmatic analysis
# - text: Human-readable text format
# - both: Save both formats
LOG_FORMAT=both
